// AUTO-GENERATED by scripts/generate-commands.mjs - DO NOT EDIT
import { defineCommand } from "citty";
import type { TimrClient } from "timr-sdk";
import { globalArgs, printJson, readBody, resolveClient, numOrUndef, csv } from "../_shared.js";

const cmd_create = defineCommand({
  meta: { name: "create", description: "create a Project Time" },
  args: {
    ...globalArgs,
    data: { type: "string", description: "Request body as JSON string, @file.json, or '-' for stdin" },
  },
  async run({ args }) {
    const client = await resolveClient(args);
    const { data } = await (client as TimrClient).POST("/project-times" as never, { body: (await readBody(args.data)) as never } as never);
    printJson(data);
  },
});

const cmd_delete = defineCommand({
  meta: { name: "delete", description: "deletes a single Project Time" },
  args: {
    ...globalArgs,
    "id": { type: "positional", description: "project-time id", required: true },
  },
  async run({ args }) {
    const client = await resolveClient(args);
    const { data } = await (client as TimrClient).DELETE("/project-times/{id}" as never, { params: { path: { "id": args["id"] as string } } } as never);
    printJson(data);
  },
});

const cmd_get = defineCommand({
  meta: { name: "get", description: "gets a single Project Time" },
  args: {
    ...globalArgs,
    "id": { type: "positional", description: "project-time id", required: true },
  },
  async run({ args }) {
    const client = await resolveClient(args);
    const { data } = await (client as TimrClient).GET("/project-times/{id}" as never, { params: { path: { "id": args["id"] as string } } } as never);
    printJson(data);
  },
});

const cmd_list = defineCommand({
  meta: { name: "list", description: "fetches Project Times" },
  args: {
    ...globalArgs,
    "notes": { type: "string", description: "pass a search string to filter entries containing this string in their notes" },
    "start-from": { type: "string", description: "pass a date to filter entries started from this day on" },
    "start-to": { type: "string", description: "pass a date to filter entries started up to (including) this day" },
    "last-modified-after": { type: "string", description: "pass a datetime to filter entries modified after this instant" },
    "task": { type: "string", description: "pass a tasks id to restrict entries based on its value" },
    "traverse-task-tree": { type: "boolean", description: "pass this parameter with value true/false to filter just by task specified or also traverse its task tree" },
    "billable": { type: "boolean", description: "pass this parameter with value true/false to show just entries/no entries which are billable" },
    "statuses": { type: "string", description: "pass one or more statuses to filter entries based on their status" },
    "users": { type: "string", description: "pass one or more user ids to restrict entries based on the user they are associated to" },
    "changed": { type: "boolean", description: "pass a boolean to filter entries based on whether they have been changed by the user or not" },
    "page-token": { type: "string", description: "Token for retrieving the next page. This token is provided in the response of the previous page as next_page_token" },
    "limit": { type: "string", description: "pass a value to change the maximum number of entries returned in a single page (at max 500)." },
    "sort-direction": { type: "string", description: "pass a direction in which the entries returned will be sorted in" },
    "sort-by": { type: "string", description: "By default sorted by: * start * Id" },
  },
  async run({ args }) {
    const client = await resolveClient(args);
    const { data } = await (client as TimrClient).GET("/project-times" as never, { params: { query: { "notes": args["notes"] as string | undefined, "start_from": args["start-from"] as string | undefined, "start_to": args["start-to"] as string | undefined, "last_modified_after": args["last-modified-after"] as string | undefined, "task": args["task"] as string | undefined, "traverse_task_tree": args["traverse-task-tree"] as boolean | undefined, "billable": args["billable"] as boolean | undefined, "statuses": csv(args["statuses"]), "users": csv(args["users"]), "changed": args["changed"] as boolean | undefined, "page_token": args["page-token"] as string | undefined, "limit": numOrUndef(args["limit"]), "sort_direction": args["sort-direction"] as string | undefined, "sort_by": args["sort-by"] as string | undefined } } } as never);
    printJson(data);
  },
});

const cmd_list_deleted = defineCommand({
  meta: { name: "list-deleted", description: "Get deleted project times" },
  args: {
    ...globalArgs,
    "last-modified": { type: "string", description: "optionally, pass a datetime to filter entries modified after this instant" },
    "page-token": { type: "string", description: "Token for retrieving the next page. This token is provided in the response of the previous page as next_page_token" },
    "limit": { type: "string", description: "optionally, pass a value to change the maximum number of entries returned in a single page (at max 500)." },
    "sort-direction": { type: "string", description: "optionally, pass a direction in which the entries returned will be sorted in" },
    "sort-by": { type: "string", description: "By default sorted by: * last_modified * Id optionally, pass a property by which the returned entries will be sorted if you want a different order" },
  },
  async run({ args }) {
    const client = await resolveClient(args);
    const { data } = await (client as TimrClient).GET("/project-times:deleted" as never, { params: { query: { "last_modified": args["last-modified"] as string | undefined, "page_token": args["page-token"] as string | undefined, "limit": numOrUndef(args["limit"]), "sort_direction": args["sort-direction"] as string | undefined, "sort_by": args["sort-by"] as string | undefined } } } as never);
    printJson(data);
  },
});

const cmd_update = defineCommand({
  meta: { name: "update", description: "updates a single Project Time" },
  args: {
    ...globalArgs,
    "id": { type: "positional", description: "project-time id", required: true },
    data: { type: "string", description: "Request body as JSON string, @file.json, or '-' for stdin" },
  },
  async run({ args }) {
    const client = await resolveClient(args);
    const { data } = await (client as TimrClient).PATCH("/project-times/{id}" as never, { params: { path: { "id": args["id"] as string } }, body: (await readBody(args.data)) as never } as never);
    printJson(data);
  },
});

export const projectTimesCommand = defineCommand({
  meta: { name: "project-times", description: "Manage project-times" },
  subCommands: {
    "create": cmd_create,
    "delete": cmd_delete,
    "get": cmd_get,
    "list": cmd_list,
    "list-deleted": cmd_list_deleted,
    "update": cmd_update,
  },
});
