// AUTO-GENERATED by scripts/generate-commands.mjs - DO NOT EDIT
import { defineCommand } from "citty";
import type { TimrClient } from "timr-sdk";
import { globalArgs, printJson, readBody, resolveClient, numOrUndef, csv } from "../_shared.js";

const cmd_delete = defineCommand({
  meta: { name: "delete", description: "deletes a single Working Time Request" },
  args: {
    ...globalArgs,
    "id": { type: "positional", description: "working-time-request id", required: true },
  },
  async run({ args }) {
    const client = await resolveClient(args);
    const { data } = await (client as TimrClient).DELETE("/working-time-requests/{id}" as never, { params: { path: { "id": args["id"] as string } } } as never);
    printJson(data);
  },
});

const cmd_get = defineCommand({
  meta: { name: "get", description: "gets a single Working Time Request" },
  args: {
    ...globalArgs,
    "id": { type: "positional", description: "working-time-request id", required: true },
  },
  async run({ args }) {
    const client = await resolveClient(args);
    const { data } = await (client as TimrClient).GET("/working-time-requests/{id}" as never, { params: { path: { "id": args["id"] as string } } } as never);
    printJson(data);
  },
});

const cmd_list = defineCommand({
  meta: { name: "list", description: "fetches Working Time Requests" },
  args: {
    ...globalArgs,
    "from": { type: "string", description: "pass a date to filter entries started from this day on" },
    "to": { type: "string", description: "pass a date to filter entries started up to (including) this day" },
    "last-modified-after": { type: "string", description: "pass a datetime to filter entries modified after this instant" },
    "working-time-types": { type: "string", description: "pass one or more ids of Working Time Types to restrict entries based on these types" },
    "statuses": { type: "string", description: "pass one or more statuses to filter entries based on their status" },
    "users": { type: "string", description: "pass one or more user ids to restrict entries based on the user they are associated to" },
    "substitutes": { type: "string", description: "pass one or more user ids to restrict entries based on the substitute they are associated to" },
    "page-token": { type: "string", description: "Token for retrieving the next page. This token is provided in the response of the previous page as next_page_token" },
    "limit": { type: "string", description: "pass a value to change the maximum number of entries returned in a single page (at max 500)." },
    "sort-direction": { type: "string", description: "pass a direction in which the entries returned will be sorted in" },
    "sort-by": { type: "string", description: "By default sorted by: * start * Id" },
  },
  async run({ args }) {
    const client = await resolveClient(args);
    const { data } = await (client as TimrClient).GET("/working-time-requests" as never, { params: { query: { "from": args["from"] as string | undefined, "to": args["to"] as string | undefined, "last_modified_after": args["last-modified-after"] as string | undefined, "working_time_types": csv(args["working-time-types"]), "statuses": csv(args["statuses"]), "users": csv(args["users"]), "substitutes": csv(args["substitutes"]), "page_token": args["page-token"] as string | undefined, "limit": numOrUndef(args["limit"]), "sort_direction": args["sort-direction"] as string | undefined, "sort_by": args["sort-by"] as string | undefined } } } as never);
    printJson(data);
  },
});

const cmd_list_deleted = defineCommand({
  meta: { name: "list-deleted", description: "Get deleted working time requests" },
  args: {
    ...globalArgs,
    "last-modified": { type: "string", description: "optionally, pass a datetime to filter entries modified after this instant" },
    "page-token": { type: "string", description: "Token for retrieving the next page. This token is provided in the response of the previous page as next_page_token" },
    "limit": { type: "string", description: "optionally, pass a value to change the maximum number of entries returned in a single page (at max 500)." },
    "sort-direction": { type: "string", description: "optionally, pass a direction in which the entries returned will be sorted in" },
    "sort-by": { type: "string", description: "By default sorted by: * last_modified * Id optionally, pass a property by which the returned entries will be sorted if you want a different order" },
  },
  async run({ args }) {
    const client = await resolveClient(args);
    const { data } = await (client as TimrClient).GET("/working-time-requests:deleted" as never, { params: { query: { "last_modified": args["last-modified"] as string | undefined, "page_token": args["page-token"] as string | undefined, "limit": numOrUndef(args["limit"]), "sort_direction": args["sort-direction"] as string | undefined, "sort_by": args["sort-by"] as string | undefined } } } as never);
    printJson(data);
  },
});

export const workingTimeRequestsCommand = defineCommand({
  meta: { name: "working-time-requests", description: "Manage working-time-requests" },
  subCommands: {
    "delete": cmd_delete,
    "get": cmd_get,
    "list": cmd_list,
    "list-deleted": cmd_list_deleted,
  },
});
