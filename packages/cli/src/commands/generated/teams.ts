// AUTO-GENERATED by scripts/generate-commands.mjs - DO NOT EDIT
import { defineCommand } from "citty";
import type { TimrClient } from "timr-sdk";
import { globalArgs, printJson, readBody, resolveClient, numOrUndef, csv } from "../_shared.js";

const cmd_add_leader = defineCommand({
  meta: { name: "add-leader", description: "adds a Team Leader" },
  args: {
    ...globalArgs,
    "team-id": { type: "positional", description: "team team_id", required: true },
    "user-id": { type: "positional", description: "leader user_id", required: true },
  },
  async run({ args }) {
    const client = await resolveClient(args);
    const { data } = await (client as TimrClient).PUT("/teams/{team_id}/leaders/{user_id}" as never, { params: { path: { "team_id": args["team-id"] as string, "user_id": args["user-id"] as string } } } as never);
    printJson(data);
  },
});

const cmd_add_member = defineCommand({
  meta: { name: "add-member", description: "adds a Team Member" },
  args: {
    ...globalArgs,
    "team-id": { type: "positional", description: "team team_id", required: true },
    "user-id": { type: "positional", description: "member user_id", required: true },
  },
  async run({ args }) {
    const client = await resolveClient(args);
    const { data } = await (client as TimrClient).PUT("/teams/{team_id}/members/{user_id}" as never, { params: { path: { "team_id": args["team-id"] as string, "user_id": args["user-id"] as string } } } as never);
    printJson(data);
  },
});

const cmd_create = defineCommand({
  meta: { name: "create", description: "create a Team" },
  args: {
    ...globalArgs,
    data: { type: "string", description: "Request body as JSON string, @file.json, or '-' for stdin" },
  },
  async run({ args }) {
    const client = await resolveClient(args);
    const { data } = await (client as TimrClient).POST("/teams" as never, { body: (await readBody(args.data)) as never } as never);
    printJson(data);
  },
});

const cmd_delete = defineCommand({
  meta: { name: "delete", description: "deletes a single Team" },
  args: {
    ...globalArgs,
    "id": { type: "positional", description: "team id", required: true },
  },
  async run({ args }) {
    const client = await resolveClient(args);
    const { data } = await (client as TimrClient).DELETE("/teams/{id}" as never, { params: { path: { "id": args["id"] as string } } } as never);
    printJson(data);
  },
});

const cmd_get = defineCommand({
  meta: { name: "get", description: "get a single team" },
  args: {
    ...globalArgs,
    "id": { type: "positional", description: "team id", required: true },
  },
  async run({ args }) {
    const client = await resolveClient(args);
    const { data } = await (client as TimrClient).GET("/teams/{id}" as never, { params: { path: { "id": args["id"] as string } } } as never);
    printJson(data);
  },
});

const cmd_get_leader = defineCommand({
  meta: { name: "get-leader", description: "gets a single Team Leader" },
  args: {
    ...globalArgs,
    "team-id": { type: "positional", description: "team team_id", required: true },
    "user-id": { type: "positional", description: "leader user_id", required: true },
  },
  async run({ args }) {
    const client = await resolveClient(args);
    const { data } = await (client as TimrClient).GET("/teams/{team_id}/leaders/{user_id}" as never, { params: { path: { "team_id": args["team-id"] as string, "user_id": args["user-id"] as string } } } as never);
    printJson(data);
  },
});

const cmd_get_member = defineCommand({
  meta: { name: "get-member", description: "gets a single Team Member" },
  args: {
    ...globalArgs,
    "team-id": { type: "positional", description: "team team_id", required: true },
    "user-id": { type: "positional", description: "member user_id", required: true },
  },
  async run({ args }) {
    const client = await resolveClient(args);
    const { data } = await (client as TimrClient).GET("/teams/{team_id}/members/{user_id}" as never, { params: { path: { "team_id": args["team-id"] as string, "user_id": args["user-id"] as string } } } as never);
    printJson(data);
  },
});

const cmd_list = defineCommand({
  meta: { name: "list", description: "fetches Teams" },
  args: {
    ...globalArgs,
    "name": { type: "string", description: "pass a group name to filter entries by the group name, matching any segment in the breadcrumbs path" },
    "page-token": { type: "string", description: "Token for retrieving the next page. This token is provided in the response of the previous page as next_page_token" },
    "limit": { type: "string", description: "pass a value to change the maximum number of entries returned in a single page (at max 500)." },
    "sort-direction": { type: "string", description: "pass a direction in which the entries returned will be sorted in (based on the Group's name)" },
    "sort-by": { type: "string", description: "By default sorted by: * name * id pass a property by which the returned entries will be sorted if you want a different order" },
  },
  async run({ args }) {
    const client = await resolveClient(args);
    const { data } = await (client as TimrClient).GET("/teams" as never, { params: { query: { "name": args["name"] as string | undefined, "page_token": args["page-token"] as string | undefined, "limit": numOrUndef(args["limit"]), "sort_direction": args["sort-direction"] as string | undefined, "sort_by": args["sort-by"] as string | undefined } } } as never);
    printJson(data);
  },
});

const cmd_list_leaders = defineCommand({
  meta: { name: "list-leaders", description: "fetches Team Leaders" },
  args: {
    ...globalArgs,
    "id": { type: "positional", description: "team id", required: true },
    "page-token": { type: "string", description: "Token for retrieving the next page. This token is provided in the response of the previous page as next_page_token" },
    "limit": { type: "string", description: "pass a value to change the maximum number of entries returned in a single page (at max 500)." },
    "sort-direction": { type: "string", description: "pass a direction in which the entries returned will be sorted in" },
    "sort-by": { type: "string", description: "By default sorted by: * Lastname * Firstname * Id of the assigned user" },
  },
  async run({ args }) {
    const client = await resolveClient(args);
    const { data } = await (client as TimrClient).GET("/teams/{id}/leaders" as never, { params: { path: { "id": args["id"] as string }, query: { "page_token": args["page-token"] as string | undefined, "limit": numOrUndef(args["limit"]), "sort_direction": args["sort-direction"] as string | undefined, "sort_by": args["sort-by"] as string | undefined } } } as never);
    printJson(data);
  },
});

const cmd_list_members = defineCommand({
  meta: { name: "list-members", description: "fetches Team Members" },
  args: {
    ...globalArgs,
    "id": { type: "positional", description: "team id", required: true },
    "page-token": { type: "string", description: "Token for retrieving the next page. This token is provided in the response of the previous page as next_page_token" },
    "limit": { type: "string", description: "pass a value to change the maximum number of entries returned in a single page (at max 500)." },
    "sort-direction": { type: "string", description: "pass a direction in which the entries returned will be sorted in" },
    "sort-by": { type: "string", description: "By default sorted by: * Lastname * Firstname * Id of the assigned user" },
  },
  async run({ args }) {
    const client = await resolveClient(args);
    const { data } = await (client as TimrClient).GET("/teams/{id}/members" as never, { params: { path: { "id": args["id"] as string }, query: { "page_token": args["page-token"] as string | undefined, "limit": numOrUndef(args["limit"]), "sort_direction": args["sort-direction"] as string | undefined, "sort_by": args["sort-by"] as string | undefined } } } as never);
    printJson(data);
  },
});

const cmd_remove_leader = defineCommand({
  meta: { name: "remove-leader", description: "removes a Team Leader" },
  args: {
    ...globalArgs,
    "team-id": { type: "positional", description: "team team_id", required: true },
    "user-id": { type: "positional", description: "leader user_id", required: true },
  },
  async run({ args }) {
    const client = await resolveClient(args);
    const { data } = await (client as TimrClient).DELETE("/teams/{team_id}/leaders/{user_id}" as never, { params: { path: { "team_id": args["team-id"] as string, "user_id": args["user-id"] as string } } } as never);
    printJson(data);
  },
});

const cmd_remove_member = defineCommand({
  meta: { name: "remove-member", description: "removes a Team Member" },
  args: {
    ...globalArgs,
    "team-id": { type: "positional", description: "team team_id", required: true },
    "user-id": { type: "positional", description: "member user_id", required: true },
  },
  async run({ args }) {
    const client = await resolveClient(args);
    const { data } = await (client as TimrClient).DELETE("/teams/{team_id}/members/{user_id}" as never, { params: { path: { "team_id": args["team-id"] as string, "user_id": args["user-id"] as string } } } as never);
    printJson(data);
  },
});

const cmd_update = defineCommand({
  meta: { name: "update", description: "updates a single Team" },
  args: {
    ...globalArgs,
    "id": { type: "positional", description: "team id", required: true },
    data: { type: "string", description: "Request body as JSON string, @file.json, or '-' for stdin" },
  },
  async run({ args }) {
    const client = await resolveClient(args);
    const { data } = await (client as TimrClient).PATCH("/teams/{id}" as never, { params: { path: { "id": args["id"] as string } }, body: (await readBody(args.data)) as never } as never);
    printJson(data);
  },
});

export const teamsCommand = defineCommand({
  meta: { name: "teams", description: "Manage teams" },
  subCommands: {
    "add-leader": cmd_add_leader,
    "add-member": cmd_add_member,
    "create": cmd_create,
    "delete": cmd_delete,
    "get": cmd_get,
    "get-leader": cmd_get_leader,
    "get-member": cmd_get_member,
    "list": cmd_list,
    "list-leaders": cmd_list_leaders,
    "list-members": cmd_list_members,
    "remove-leader": cmd_remove_leader,
    "remove-member": cmd_remove_member,
    "update": cmd_update,
  },
});
