// AUTO-GENERATED by scripts/generate-commands.mjs - DO NOT EDIT
import { defineCommand } from "citty";
import type { TimrClient } from "timr-sdk";
import { globalArgs, printJson, readBody, resolveClient, numOrUndef, csv } from "../_shared.js";

const cmd_get = defineCommand({
  meta: { name: "get", description: "gets a single Holiday Calendar" },
  args: {
    ...globalArgs,
    "id": { type: "positional", description: "holiday-calendar id", required: true },
  },
  async run({ args }) {
    const client = await resolveClient(args);
    const { data } = await (client as TimrClient).GET("/holiday-calendars/{id}" as never, { params: { path: { "id": args["id"] as string } } } as never);
    printJson(data);
  },
});

const cmd_list = defineCommand({
  meta: { name: "list", description: "fetches Holiday Calendars" },
  args: {
    ...globalArgs,
    "page-token": { type: "string", description: "Token for retrieving the next page. This token is provided in the response of the previous page as next_page_token" },
    "limit": { type: "string", description: "pass a value to change the maximum number of entries returned in a single page (at max 500)." },
    "sort-direction": { type: "string", description: "pass a direction in which the entries returned will be sorted in" },
    "sort-by": { type: "string", description: "By default sorted by: * Description * Id" },
  },
  async run({ args }) {
    const client = await resolveClient(args);
    const { data } = await (client as TimrClient).GET("/holiday-calendars" as never, { params: { query: { "page_token": args["page-token"] as string | undefined, "limit": numOrUndef(args["limit"]), "sort_direction": args["sort-direction"] as string | undefined, "sort_by": args["sort-by"] as string | undefined } } } as never);
    printJson(data);
  },
});

const cmd_list_holidays = defineCommand({
  meta: { name: "list-holidays", description: "fetches all Holidays of a specific Holiday Calendar" },
  args: {
    ...globalArgs,
    "id": { type: "positional", description: "holiday-calendar id", required: true },
    "page-token": { type: "string", description: "Token for retrieving the next page. This token is provided in the response of the previous page as next_page_token" },
    "limit": { type: "string", description: "pass a value to change the maximum number of entries returned in a single page (at max 500)." },
    "sort-direction": { type: "string", description: "pass a direction in which the entries returned will be sorted in" },
    "sort-by": { type: "string", description: "By default sorted by: * Description * Id" },
  },
  async run({ args }) {
    const client = await resolveClient(args);
    const { data } = await (client as TimrClient).GET("/holiday-calendars/{id}/holidays" as never, { params: { path: { "id": args["id"] as string }, query: { "page_token": args["page-token"] as string | undefined, "limit": numOrUndef(args["limit"]), "sort_direction": args["sort-direction"] as string | undefined, "sort_by": args["sort-by"] as string | undefined } } } as never);
    printJson(data);
  },
});

export const holidayCalendarsCommand = defineCommand({
  meta: { name: "holiday-calendars", description: "Manage holiday-calendars" },
  subCommands: {
    "get": cmd_get,
    "list": cmd_list,
    "list-holidays": cmd_list_holidays,
  },
});
